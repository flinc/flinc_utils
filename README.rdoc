= flinc utils

A collection of (more or less) useful utilities used at flinc.

== mensa.rb

Displays today's menu at Mensa Dieburg.

=== Install

A C compiler is required to install nokogiri. On Mac OS X just install XCode.

  sudo gem install nokogiri
  chmod +x mensa.rb

=== Usage

  ./mensa.rb